import BossButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
BossButtonGroup.install = function(Vue) {
  Vue.component(BossButtonGroup.name, BossButtonGroup);
};

export default BossButtonGroup;
