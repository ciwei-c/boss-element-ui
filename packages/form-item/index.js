import BossFormItem from '../form/src/form-item';

/* istanbul ignore next */
BossFormItem.install = function(Vue) {
  Vue.component(BossFormItem.name, BossFormItem);
};

export default BossFormItem;
