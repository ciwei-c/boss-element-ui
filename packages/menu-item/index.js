import BossMenuItem from '../menu/src/menu-item';

/* istanbul ignore next */
BossMenuItem.install = function(Vue) {
  Vue.component(BossMenuItem.name, BossMenuItem);
};

export default BossMenuItem;
